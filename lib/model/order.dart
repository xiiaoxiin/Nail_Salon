class Order {
  String phone, date, time, address, payMethod, payOption, payAmount, totalPayable;
  Order({
    this.phone,
    this.date,
    this.time,
    this.address,
    this.payMethod,
    this.payOption,
    this.payAmount,
    this.totalPayable,
  });
}